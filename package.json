{
  "name": "react-with-rest-api",
  "version": "0.1.0",
  "description": "This is a course management app.",
  "scripts": {
    "build": "cd api && npm install && npm start",
    "heroku-postbuild": "cd client && npm install && npm start"
  },
  "keywords": [
    "react",
    "node",
    "heroku"
  ]  
}
